import React from "react";
import { Container, Text } from "native-base";

const EnrolledGroups = ({ navigation }) => {
  return (
      <Container>
          <Text>Enrolled Groups</Text>
      </Container>
  );
};

export default EnrolledGroups;
